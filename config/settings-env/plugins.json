{
    "mgmt_public_hostport": "${MGMT_BACKEND_HOST}:${MGMT_BACKEND_PUBLICPORT}",
    "mgmt_private_hostport": "${MGMT_BACKEND_HOST}:${MGMT_BACKEND_PRIVATEPORT}",
    "model_public_hostport": "${MODEL_BACKEND_HOST}:${MODEL_BACKEND_PUBLICPORT}",
    "model_private_hostport": "${MODEL_BACKEND_HOST}:${MODEL_BACKEND_PRIVATEPORT}",
    "registry_hostport": "${REGISTRY_HOST}:${REGISTRY_PORT}"
}
