{
  "debug": "https://${API_GATEWAY_HOST}:${API_GATEWAY_PORT}",
  "pipeline": "http://${PIPELINE_BACKEND_HOST}:${PIPELINE_BACKEND_PUBLICPORT}",
  "connector": "http://${CONNECTOR_BACKEND_HOST}:${CONNECTOR_BACKEND_PUBLICPORT}"
}